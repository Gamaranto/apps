import React from "react";
import ReactDOM from "react-dom";

export default function App() {
  return (
    <div>
      {/* <Button size="large" color="primary" onClick={() => console.log("Works 🎉")}>
        Test if it works
      </Button> */}
    </div>
  );
}

ReactDOM.render(<App />, document.getElementById("root"));
