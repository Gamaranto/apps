export declare type AvatarStyleProps = {
    img?: string;
    size?: "small" | "default" | "large";
};
export declare let makeStyles: ({ img, size }: AvatarStyleProps) => import("@emotion/utils").SerializedStyles;
